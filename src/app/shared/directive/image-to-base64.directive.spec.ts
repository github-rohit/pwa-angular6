import { ImageToBase64Directive } from './image-to-base64.directive';

describe('ImageToBase64Directive', () => {
  it('should create an instance', () => {
    const directive = new ImageToBase64Directive();
    expect(directive).toBeTruthy();
  });
});
